% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aoi.R
\name{read_aoi}
\alias{read_aoi}
\title{Read an area of interest}
\usage{
read_aoi(
  aoi,
  layer = NULL,
  dissolve = TRUE,
  make_valid = TRUE,
  target_crs = NULL
)
}
\arguments{
\item{aoi}{A filename, \code{sf}, \code{sfc}, or \code{terra::SpatVector} object.}

\item{layer}{Optional layer name when reading a multi-layer file such
as a GeoPackage.}

\item{dissolve}{Logical. If \code{TRUE}, combine all features into one AOI.}

\item{make_valid}{Logical. If \code{TRUE}, attempt to repair invalid geometry.}

\item{target_crs}{Optional output coordinate reference system accepted
by \code{terra::project()}, for example \code{"EPSG:27700"}.}
}
\value{
A \code{terra::SpatVector}.
}
\description{
Reads an area of interest from a vector file, an \code{sf} object, an
\code{sfc} object, or a \code{terra::SpatVector}.
}
\details{
Multiple features are optionally combined into a single geometry,
and invalid geometries are repaired where possible.
}
\examples{
\dontrun{
aoi <- read_aoi("study_area.gpkg")

aoi_bng <- read_aoi(
  "study_area.gpkg",
  target_crs = "EPSG:27700"
)
}

}
